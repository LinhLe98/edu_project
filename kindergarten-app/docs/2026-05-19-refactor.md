# 2026-05-19 — Refactor: mamnon → kindergarten + Structure Cleanup

## Goal
Rename all `mamnon` identifiers to `kindergarten` for consistency, and refactor the project structure to a cleaner, more maintainable format.

## Changes Made

### Phase 1 — Rename `mamnon` → `kindergarten`

| File | Change |
|------|--------|
| `angular.json` | Project key `"mamnon-app"` → `"kindergarten-app"` (3 occurrences) |
| `README.md` | `# MamnonApp` → `# KindergartenApp` |
| `CLAUDE.md` | `cd mamnon-app`, `dist/mamnon-app/browser/` → `kindergarten-app` variants |
| `docs/2026-05-09-setup.md` | Historical setup command updated |
| `package-lock.json` | Deleted and regenerated via `npm install` |
| `dist/mamnon-app/` | Deleted (stale build output) |

**Note:** `package.json` was already updated to `"name": "kindergarten-app"` prior to this refactor. No TypeScript source files contained `mamnon` identifiers — rename was config/docs only.

### Phase 2 — Structural Refactor: `pages/` → `features/`

Renamed `src/app/pages/` → `src/app/features/`. This aligns with idiomatic Angular naming (routed feature areas are called "features", not "pages").

- Updated all 9 lazy `loadComponent` import paths in `src/app/app.routes.ts`
- No other files outside `app.routes.ts` imported from the `pages/` directory

**Directory structure after rename:**
```
src/app/
  core/
    models/    ← unchanged
    data/      ← unchanged
    services/  ← unchanged + new index.ts barrel
  shared/      ← unchanged
  features/    ← was pages/
    home/
    about/
    activities/
    library/
    contact/
    news/
    teacher/
    teacher-group/
```

### Phase 3 — Add `core/services/index.ts` Barrel

Created `src/app/core/services/index.ts` to mirror the existing `core/models/index.ts` pattern. Exports all 8 services. Components can now import from `'../../core/services'` instead of individual service file paths.

## Verification

```
# 1. No remaining "mamnon" references
grep -ri "mamnon" src/ angular.json package.json CLAUDE.md README.md docs/
→ (no output — clean)

# 2. No remaining "pages/" references in routes
grep "pages/" src/app/app.routes.ts
→ (no output — clean)

# 3. Build output location
npx ng build --configuration development
→ Output location: dist/kindergarten-app  ✓
```

Build completed with zero errors. One pre-existing template warning in `ImageLightboxComponent` (unrelated to this refactor).
