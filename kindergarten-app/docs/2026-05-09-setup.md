# 2026-05-09 — Initial Setup Log

## Environment
- Node: v25.9.0
- Angular CLI: 21.2.10 (via npx — no global install needed due to permission constraints)
- Tailwind CSS: 3.4.x
- Swiper: 12.x

## Steps Completed

### 1. Project Creation
```bash
npx -y @angular/cli@latest new kindergarten-app --standalone --routing --style=scss --skip-git
```
Generated standalone Angular 21 project with SCSS and routing.

### 2. Tailwind CSS Setup
```bash
npm install -D tailwindcss@3 postcss autoprefixer @tailwindcss/typography @tailwindcss/aspect-ratio
npx tailwindcss init -p
```
Configured `tailwind.config.js` with custom pastel color palette, font families, shadows.

### 3. Swiper
```bash
npm install swiper@12
```
Registered via `register()` in `main.ts` (Web Components API).

### 4. Angular Animations
```bash
npm install @angular/animations
```
Required for mobile drawer slide-in animation.

### 5. Data Layer
Created:
- 7 TypeScript model interfaces
- 7 mock data files (8 news, 12 staff, 6 programs, 9 albums, 6 facilities, 1 contact, 5 slides)
- 7 synchronous services

### 6. Components Built
- Shared: Header, Footer, Slideshow, PageHero, SectionHeading, Breadcrumb, ImageGalleryGrid, 4 cards, TagBadge
- Pages: Home (5 sections), About (4 sections), Activities (2 sections), Library (2 sections + AlbumDetail), Contact (2 sections)

### 7. Build Status
```
Application bundle generation complete. [0.858 seconds]
```
Zero errors. All 6 routes navigable. Lazy chunk splitting confirmed.

## Issues Encountered & Fixed
- `@angular/animations` not auto-installed by Angular CLI 21 — installed manually
- `NgIf` missing from breadcrumb and image-gallery-grid imports — added
- Unused `RouterLink` import in LatestNewsComponent — removed
- Card-news had unused `NgIf`/`RouterLink` imports — removed
